
export type RegisterFormType = {
    username: string,
    email: string, 
    password: string
};

export type LoginFormType = {
    email: string, 
    password: string,
    redirectTo: string
};

